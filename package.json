{
  "name": "aegisos",
  "version": "1.0.0",
  "private": true,
  "description": "AegisOS — AI automation OS for DeFi safety + yield",
  "scripts": {
    "dev": "npm run build:shared && npm run dev --prefix frontend",
    "dev:win": "npm run build:shared & npm run dev --prefix frontend",
    "build:shared": "npm run build --prefix shared",
    "build": "npm run build:shared && npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "install:all": "npm install --prefix shared && npm run build --prefix shared && npm install --prefix frontend"
  },
  "devDependencies": {}
}
